package com.ContractBilling.commissions.entity;

/**
 * Pay type for contractor placements
 */
public enum PayType {
    HOURLY,   // Paid by the hour
    SALARY    // Annual salary (converted to hourly for calculations)
}
